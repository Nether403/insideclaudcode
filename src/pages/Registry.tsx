import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Shield } from "lucide-react";
export default function Registry() {
  return <PlaceholderPage title="Codename Registry" subtitle="Searchable database of all discovered model codenames and specifications." icon={Shield} tag="DATABASE" />;
}
