import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Clock } from "lucide-react";
export default function Timeline() {
  return <PlaceholderPage title="Interactive Timeline" subtitle="Chronological breakdown of the leak discovery, archiving, and community response." icon={Clock} tag="PHASE 2" />;
}
