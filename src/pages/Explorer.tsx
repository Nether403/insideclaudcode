import { PlaceholderPage } from "@/components/PlaceholderPage";
import { GitBranch } from "lucide-react";
export default function Explorer() {
  return <PlaceholderPage title="Architecture Explorer" subtitle="Interactive system diagram with expandable nodes and code snippets." icon={GitBranch} tag="INTERACTIVE" />;
}
