import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Search } from "lucide-react";
export default function SearchPage() {
  return <PlaceholderPage title="Search" subtitle="Full-text search across all content sections and findings." icon={Search} tag="UTILITY" />;
}
