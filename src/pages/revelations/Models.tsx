import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Database } from "lucide-react";
export default function Models() {
  return <PlaceholderPage title="Model Registry" subtitle="Unreleased models: Fennec, Capybara, Tangu, Numbat and their specifications." icon={Database} tag="DISCOVERED" />;
}
