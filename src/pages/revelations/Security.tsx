import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Lock } from "lucide-react";
export default function Security() {
  return <PlaceholderPage title="Security & Safety" subtitle="YOLO classifier, bash AST analysis, unicode smuggling prevention, ptrace protection." icon={Lock} tag="SENSITIVE" />;
}
