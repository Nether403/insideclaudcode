import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Zap } from "lucide-react";
export default function Features() {
  return <PlaceholderPage title="Hidden Features" subtitle="Buddy, KAIROS, Ultraplan, Coordinator — the unreleased tools inside Claude." icon={Zap} tag="UNRELEASED" />;
}
